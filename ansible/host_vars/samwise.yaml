---
# k3s version samwise's agent installs. MUST match gondor's server version.
# Three lockstep locations today (the planned install-k3s.sh -> ansible refactor
# in ROADMAP "Generalize / refactor" will collapse these to one):
#   1. this file                              (agent install)
#   2. gondor/bootstrap/install-k3s.sh        (server install fallback)
#   3. justfile (top-line `k3s_version :=`)   (overrides #2 via env var)
# Bump all three together until the refactor lands.
k3s_version: v1.34.6+k3s1
