apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helmrepository.yaml
  - helmrelease.yaml
  # Backup/recovery plugin for the operator above — same namespace, same
  # HelmRepository. See the header in barman-plugin-helmrelease.yaml.
  - barman-plugin-helmrelease.yaml
