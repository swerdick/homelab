# This manifest was generated by flux. DO NOT EDIT.
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 1m0s
  provider: github
  ref:
    branch: main
  secretRef:
    name: flux-system
  url: https://github.com/swerdick/homelab.git
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./kubernetes
  # TEMPORARY (apps-promotion step 1/3): GC disarmed while the apps tree
  # moves from this Kustomization to a dedicated one — per the Flux FAQ
  # procedure for moving resources between Kustomizations. Restored in 3/3.
  prune: false
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
